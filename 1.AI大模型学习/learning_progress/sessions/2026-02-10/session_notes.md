# 2026-02-10 复习会话记录

## 1. 知识点摘要
- **核心主题**：模型高效部署 (vLLM) 与业务架构优化
- **关键技术**：
    - **vLLM 核心**：PageAttention (显存管理), Continuous Batching (吞吐优化), Shared Base Model (多 LoRA 支持)。
    - **架构优化**：沙漏式/混合架构 (规则过滤 + 大模型判决)。
    - **Prompt 工程**：Batching (批处理) 降低 Token 开销。

## 2. 问答表现评估
- **整体评分**：9/10
- **表现良好**：
    - 深刻理解 vLLM 如何通过 PageAttention 解决显存碎片化问题（类比操作系统虚拟内存）。
    - 准确描述 Continuous Batching 解决 "Padding 浪费" 的机制（迭代级调度）。
    - 掌握业务层“降本增效”的核心逻辑（车系识别案例中仅 2.54% 数据需大模型）。
    - 正确理解 Batching Prompt 对均摊系统 Token 开销的价值。
    - 正确回答多 LoRA 场景下的显存优化（共享基座）。
- **薄弱环节**：
    - **KV Cache 术语**：对传统 KV Cache 的缺陷描述稍显粗糙（使用了“固定长度”，更精准应为“预分配最大可能长度”）。

## 3. 认知冲突与难点
- **KV Cache 分配**：修正了对传统 KV Cache 分配方式的描述，强调了其“预分配”导致的内部碎片和外部碎片问题。

## 4. 概念/模型对比 (Code/Model Diff)
- **Traditional Batching vs Continuous Batching**：前者受限于最长任务（短板效应），后者实现迭代级调度（随进随出）。
- **Single LoRA vs Multi-LoRA (vLLM)**：vLLM 支持单基座挂载多适配器，无需多实例部署。

## 5. 用户疑问
- (本次会话主要为面试官提问模式，用户无特定疑问)

## 6. 明日复习计划
- **优先复习（薄弱点）**：
    1. **SimHash 算法** (高优先级，遗忘)。
    2. **交叉验证原则** (高优先级，评估误区)。
    3. **KV Cache 术语** (中优先级，今日遗留)。
